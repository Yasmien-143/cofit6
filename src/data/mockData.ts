import { Member, Trainer, Payment } from '../types';

// ALL MOCK ACCOUNTS DELETED. System starts empty.
export const mockTrainers: Trainer[] = [];
export const mockMembers: Member[] = [];
export const mockPayments: Payment[] = [];
